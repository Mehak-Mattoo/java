class Solution {
    public boolean rotateString(String s, String goal) {

if(s.length()!= goal.length()){
    return false; }

s= s+s; // concatination contains all rotations
return s.contains(goal);
    }
}

