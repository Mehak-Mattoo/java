class CheckBit {
    // Function to check if Kth bit is set or not.
    static boolean checkKthBit(int n, int k) {
        if (((n >> k) & 1) != 0) { // shift the num to right by k then & it with 1
            return true;
        }
        return false;
    }
}
